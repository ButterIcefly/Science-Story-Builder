素材说明

本压缩包包含 7 张单独生成的透明 PNG 单帧：
capybara_frame_01.png ~ capybara_frame_07.png

推荐用法：
- 直接按文件名顺序循环播放。
- 默认约 150ms/帧。
- 点击后切到约 60ms/帧，持续约 1.4 秒。
- 用 <img> + JS 切换 src，不要把图片重新拼接。

附带文件：
- CLAUDE_PROMPT.txt：可直接发给 Claude Code 的提示词。
